import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Header extends Component {
    state = {  }
    render() {
      return (
        <div>Teste</div>
      );
    }
}

Header.propTypes = {/* eslint-disable */
  user: PropTypes.array,
};

export default Header;
